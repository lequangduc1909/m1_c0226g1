let numOfNumber = +prompt("Mời bạn nhập số lượng số nguyên mong muốn hiển thị: ");

if (isNaN(numOfNumber) || numOfNumber <= 0) {
  alert("Nhập lại đi bạn ơi");
} else {
  for (let count = 0; count < numOfNumber - 1; count++) {
    
    
  }
}
